{
  "name": "@orator/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 4321",
    "build": "astro build && node ../../scripts/minify-assets.mjs dist/client",
    "deploy:staging": "CLOUDFLARE_ENV=staging pnpm run build && wrangler deploy -c dist/server/wrangler.json",
    "deploy:production": "CLOUDFLARE_ENV=production pnpm run build && wrangler deploy -c dist/server/wrangler.json",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.3",
    "@orator/adapters-cf": "workspace:*",
    "@orator/core": "workspace:*",
    "@orator/protocol": "workspace:*",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10"
  }
}
