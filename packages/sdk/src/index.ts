/**
 * @orator/sdk — public TypeScript client over @orator/protocol (SPEC §53).
 * Built in Phase 4; whether it ships in MVP at all is open decision §80.17.
 */
export { PROTOCOL_VERSION } from "@orator/protocol";
